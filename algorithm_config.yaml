# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: ogc-sister-preprocess
algorithm_version: sister-dev
repository_url: https://github.com/sister-jpl/sister-preprocess.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
algorithm_description: Preprocess input radiance and ancillary datasets for downstream algorithms
# path to the build script for installing and building the algorithm in the docker image
build_command: sister-preprocess/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-preprocess/pge_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
  file:
    - name: radiance_data
      description: The input radiance file. This can be an archived radiance file (e.g. tar.gz) or an ENVI binary file.
      required: True
    - name: radiance_header
      description: The radiance header file (only used if radiance_data points to an ENVI file)
      default: None
    - name: observation_data
      description: The observation data file (only used if radiance_data points to an ENVI file)
      default: None
    - name: observation_header
      description: The observation header file (only used if radiance_data points to an ENVI file)
      default: None
    - name: location_data
      description: The location data file (only used if radiance_data points to an ENVI file)
      default: None
    - name: location_header
      description: The location header file (only used if radiance_data points to an ENVI file)
      default: None
    - name: glt_data
      description: The GLT data file (only used if radiance_data points to an ENVI file)
      default: None
    - name: glt_header
      description: The GLT header file (only used if radiance_data points to an ENVI file)
      default: None
  config:
    - name: crid
      data_type:
      default: '000'
      description:
    - name: experimental
      default: 'True'
