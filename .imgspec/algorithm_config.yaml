# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: sister-preprocess
version: 1.0.0
environment: ubuntu
repository_url: https://github.com/EnSpec/sister-preprocess.git
docker_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
description: Preprocess input radiance and ancillary datasets for downstream algorithms
# path to the build script for installing and building the algorithm in the docker image
build_command: sister-preprocess/.imgspec/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-preprocess/.imgspec/imgspec_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: l1_granule
    type: file
  - name: landsat
    type: positional
    default: None
